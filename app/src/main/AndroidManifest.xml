<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:dist="http://schemas.android.com/apk/distribution"
    package="oncreate.apps.Mathest">

    <dist:module dist:instant="true" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/mathest_logo"
        android:label="@string/app_name"
        android:usesCleartextTraffic="true"
        android:roundIcon="@mipmap/mathest_logo"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MultiplicationWorkspace"
            android:windowSoftInputMode="stateHidden"></activity>
        <activity
            android:name=".AdditionWorkspace"
            android:windowSoftInputMode="stateHidden" />
        <activity
            android:name=".LaunchScreen"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".StudentHome" />
        <activity
            android:name=".TestPage"
            android:windowSoftInputMode="stateHidden" />
        <activity
            android:name=".Login"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar"
            android:windowSoftInputMode="stateHidden" />
        <activity
            android:name=".Signup"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar"
            android:windowSoftInputMode="stateHidden" />
        <activity android:name=".StudentProfile" />
    </application>

</manifest>